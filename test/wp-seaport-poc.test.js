const { expect } = require("chai");
const { ethers } = require("hardhat");

describe("Seaport WP PoC (phase 1, manual selection)", function () {
  it("placeholder", async function () {
    expect(true).to.equal(true);
  });
});